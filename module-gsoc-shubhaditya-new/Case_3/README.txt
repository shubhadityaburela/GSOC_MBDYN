In this test we consider a cantilever beam with an absolute force applied at the node 3.
1. This time we use an array driver to linearly combine a sine driver and a logarithmic driver for input force element. 
